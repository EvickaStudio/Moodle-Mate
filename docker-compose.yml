services:
  moodlemate:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./config.ini:/app/config.ini:ro
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - TZ=UTC