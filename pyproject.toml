[tool.poetry]
name = "moodlemate"
version = "2.0.1"
description = "Moodle notification manager"
authors = ["EvickaStudio"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api" 